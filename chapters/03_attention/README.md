# 03 - 编码注意力机制

## 实现顺序

1. 无可训练参数的简单注意力；
2. 带 `W_q`、`W_k`、`W_v` 的自注意力；
3. 缩放点积与因果 mask；
4. attention dropout；
5. 多头注意力。

## 每步必须记录

- 输入、Q、K、V、分数矩阵、权重和输出的形状；
- softmax 沿哪个维度进行；
- mask 在 softmax 前还是后应用，为什么；
- `sqrt(d_k)` 的数值稳定作用。

## 门禁

与一个可信参考实现对齐小张量输出，并验证未来位置的注意力权重为零。
